generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Theme {
  id Int @id @default(autoincrement())
  name String
  imageUrl String
  previewUrl String
  category String
}